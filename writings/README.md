# Writings Local Files

The site can load writings from local markdown files at runtime.

## How to enable

1. Copy segments in `writings/index.json`.
2. Create the markdown files listed in `files`.
3. Refresh the page.

If `writings/index.json` is missing or invalid, the app falls back to the built-in writings in `app.js`.
